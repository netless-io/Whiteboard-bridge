import fs from 'fs'
let config = {
    "rpc": {
        "SDKBridge": {
            "SDKBridge": ["newWhiteSdk"]
        }
    },
    "event": {}
};
function enter() {
    // load config and parse
    console.log(config);
    // parse ast

    // gen ir

    // gen ts
}

enter();